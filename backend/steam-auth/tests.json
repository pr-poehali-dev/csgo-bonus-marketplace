{
  "tests": [
    {
      "name": "GET / без параметров — 404",
      "method": "GET",
      "path": "/",
      "expectedStatus": 404,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}
